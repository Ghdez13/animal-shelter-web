import Navbar from './Navbar'

const Header = () =>{
    return(
        <header className = "bg-white shadow-md sticky top-0 z-50">
            <Navbar/>
        </header>
    )
}

export default Header